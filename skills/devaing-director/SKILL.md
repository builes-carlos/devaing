---
name: devaing-director
description: Show project state, run health checks, and offer to execute the next step. Use when the user wants to know where they stand in the workflow, what to do next, how many tasks are done, which epic is active, or asks for project status. Can orchestrate the next skill on confirmation — the user can complete a full workflow cycle without invoking other skills manually.
---

Read `body.md` in the same directory as this SKILL.md file and execute it step by step.

The `.devaing/skills/director.md` copy in your project is for non-Claude Code runtimes (Codex, Aider, Cursor).
Claude Code always reads the globally installed body so skill updates take effect immediately.
